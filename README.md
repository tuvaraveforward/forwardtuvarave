# Forward Tuvarave — Personal Site

This repository contains a simple static personal website for Forward Tuvarave.

Overview
- Static site built with `index.html` and `css/index.css`.

How to view
- Open `index.html` in a browser to view the site locally.

Deployment
- To publish on GitHub Pages: go to the repository Settings → Pages, select the `main` branch and root, then save.

Notes
- Repo: https://github.com/tuvaraveforward/forwardtuvarave.git
- If you want, I can add a `LICENSE`, `.gitignore`, or enable GitHub Pages for you.

